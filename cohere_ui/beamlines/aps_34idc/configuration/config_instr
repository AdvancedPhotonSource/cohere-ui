specfile = "SPECFILE"
                                            // optional (but typically needed)
                                            // specfile can be specified on the command line argument to setup_34idc.py or here.

diffractometer = "34idc"
                           // it typically will be set to a defined class, but in case of new diffractometer
                           // one can enter the parameters below instead

data_dir = "ADDATADIR"
                                            // The directory containing raw data. This directory will have several
                                            // subdirectories each containing .tif data files. Each subdirectory
                                            // represents separate scan and is numbered with the scan index.

darkfield_filename = "DARKFIELD"
                                            // Dark file taken to clean bad pixels or correct background in CCD.
                                            // Depends on detector if used.

whitefield_filename = "WHITEFIELD"
                                            // White file to correct background in CCD.
                                            // Depends on detector if used.

//Imult = 10000
                                            // Optional, defaults to the average of the whitefield.
                                            // A multiplication factor used to renormalize the whitefield correction.

//roi = [0,256,0,256]
                                            // Region of interest, an area covered by detector.
                                            // Read from specfile.

// typically these parameters will be parsed from spec file
// the parsed parameters will be overridden if they are configured
//detector = "34idcTIM2"
                             // If detector is not in specfile it can be configured
//energy = 7.2
                             // wavelength
//delta = 30.1
                             // delta (degrees)
//gamma = 14.0
                             // gamma (degrees)
//detdist = 500.0
                             // camera distance (mm)
//theta = 0.1999946
                             // angular step size
//chi = 90.0
//phi = -5.0
//scanmot = "th"
//scanmot_del = 0.005
